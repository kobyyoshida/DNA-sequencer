//youtube video test
#include <iostream>

char makeArray(){
  int columns = 10;
  array = new Array(columns);
  for (int i = 0; i < array.length(); i++){
    //array[i] = new Array(rows);
    array[i] = 'X';
  }
  return array;
}
